# TODO Random

- Correct self destruct bug for KDE Splash Neofetch.
  - use check if located on itself.
  - if it is, set no self destruct mode. running script no longer clear the file itself an other metadata, but just the qml file.