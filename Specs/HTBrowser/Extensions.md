# Extensions

HT Browser will accept types of extensions

- Internal. Extension plugin for this tab manager itself. It also support manipulating resulting page like below browser specific, e.g. AdBlocker.
- Browser specific. Extension for each different browser available for the tab manager