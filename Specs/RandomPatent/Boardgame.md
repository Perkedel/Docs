# Boardgame (small note)

Standard for Tabletop Meja assets.

- Common mainstream off the shelf format. e.g. s&box is `sent` so can be used in sandbox and derivatives game mode. Contains prefab to make it spawnable as `Entity` complete with scripts. standard `Prefab` can only be spawned in editor.
- 